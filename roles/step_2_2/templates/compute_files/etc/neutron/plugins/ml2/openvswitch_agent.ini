[ovs]
local_ip = {{ overlay_ip }}
#bridge_mappings = vlan:{{ overlay_interface }},external:br-provider
# TODO: this might be needed!!!
#bridge_mappings = provider:br-provider

[agent]
tunnel_types = gre
l2_population = True

#[securitygroup]
#firewall_driver = iptables_hybrid
